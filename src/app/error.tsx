/**
 * error.tsx
 * error boundary wraps children
 * must be Client Component - add "use client" at the top of the file
 */

"use client";

const Error = () => {
  return <div>error</div>;
};

export default Error;
